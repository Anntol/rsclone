import { SortProjectsPipe } from './sort-projects.pipe';

describe('SortProjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortProjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
